apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: mavenweb
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5 
  template:
    metadata:
      labels:
        app: mavenweb
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
      - name: mavenweb
        image: nhjcontainreg.azurecr.io/mavenweb:#{Build.BuildId}#
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 250m
          limits:
            cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: mavenweb
spec:
  loadBalancerIP: 52.141.27.216
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: mavenweb
